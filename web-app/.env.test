# Test environment — committed to repo, safe to do so (no secrets).
# Vitest automatically loads this file when running in test mode.
# All tests run against mock data, never the real backend.
VITE_USE_MOCKS=true
VITE_API_BASE_URL=http://localhost:3000
