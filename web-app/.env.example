# Backend base URL. Do not commit .env with real values.
VITE_API_BASE_URL=https://api.cyclelink.example.com

# Set to 'true' to enable the local mock data layer (no network calls).
VITE_USE_MOCKS=false
